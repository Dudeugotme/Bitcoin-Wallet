apiVersion: v1
kind: Service
metadata:
  name: bitcoinwallet
  labels:
    name: bitcoinwallet
spec:
  # type: ClusterIP
  ports:
  - name: btcwalletport
    port: 8234
    targetPort: 8234
  selector:
    name: bitcoinwallet